#!/usr/bin/node
// Integrers Number
const Arguments = process.argv
const integral = parseInt(Arguments['2']);
if (!integral){
console.log('Not a number');
}
else 
console.log('My number: ' + integral);

